apps:
  - script : ./server.js
    name   : 'backend'
    watch  : true
    ignore_watch:
      - node_modules
    watch_options:
      usePolling: true
    env_production:
      NODE_ENV: production
